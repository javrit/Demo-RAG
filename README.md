This repo is a first demo of a RAG, that takes a PDF file and a prompt as input, to help you coding.
Unfortunately, it doesn't take pictures as input as I chose to use a very-low-cost OPENAI LLM to make the embeddings.

The app is available at the following URL : 

